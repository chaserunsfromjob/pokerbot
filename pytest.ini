[pytest]
# Run the suite from the top of the repository, with the repository itself on
# the import path so `import pokerbot` finds the package rather than anything
# else of that name that happens to be installed.
pythonpath = .
testpaths = tests
